<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Upload File - File Management System</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-50 text-gray-800">

    <!-- Upload Container -->
    <div class="flex items-center justify-center min-h-screen">
        <div class="w-full max-w-lg p-8 space-y-6 bg-white rounded-md shadow-xs border border-gray-300">
            <h2 class="text-3xl font-bold text-center text-gray-800">Upload New File</h2>
            
            <form action="#" method="POST" enctype="multipart/form-data" class="space-y-5">
                
                <!-- Title Field -->
                <div>
                    <label for="title" class="block text-sm font-medium text-gray-700">Title</label>
                    <input type="text" id="title" name="title" required
                           class="w-full px-4 py-2 mt-2 border border-gray-300 rounded-lg focus:outline-none focus:border-gray-500">
                </div>

                <!-- File Input -->
                <div>
                    <label for="filename" class="block text-sm font-medium text-gray-700">Choose File</label>
                    <input type="file" id="filename" name="filename" required
                           class="w-full px-4 py-2 mt-2 border border-gray-300 rounded-lg focus:outline-none focus:border-gray-500">
                </div>

                <!-- Filepath (Hidden) -->
                <input type="hidden" id="filepath" name="filepath" value="server/file/path/">
                
                <!-- Upload Date (Hidden, Auto-generated) -->
                <input type="hidden" id="uploadDate" name="uploadDate" value="{{currentDate}}">

                <!-- File Size -->
                <div>
                    <label for="size" class="block text-sm font-medium text-gray-700">File Size (Bytes)</label>
                    <input type="number" id="size" name="size" required
                           class="w-full px-4 py-2 mt-2 border border-gray-300 rounded-lg focus:outline-none focus:border-gray-500">
                </div>

                <!-- MIME Type -->
                <div>
                    <label for="type" class="block text-sm font-medium text-gray-700">File Type (MIME)</label>
                    <input type="text" id="type" name="type" placeholder="e.g., image/png" required
                           class="w-full px-4 py-2 mt-2 border border-gray-300 rounded-lg focus:outline-none focus:border-gray-500">
                </div>

                <!-- Passcode (Optional) -->
                <div>
                    <label for="passcode" class="block text-sm font-medium text-gray-700">Passcode (Optional)</label>
                    <input type="text" id="passcode" name="passcode" 
                           class="w-full px-4 py-2 mt-2 border border-gray-300 rounded-lg focus:outline-none focus:border-gray-500">
                </div>

                <!-- Encryption Toggle -->
                <div class="flex items-center">
                    <input type="checkbox" id="isEncrypted" name="isEncrypted" class="mr-2">
                    <label for="isEncrypted" class="text-sm font-medium text-gray-700">Encrypt File</label>
                </div>

                <!-- Permissions (Checkboxes) -->
                <div>
                    <label class="block text-sm font-medium text-gray-700">Permissions</label>
                    <div class="flex items-center space-x-4 mt-2">
                        <div>
                            <input type="checkbox" id="read" name="permissions[]" value="read" class="mr-2">
                            <label for="read" class="text-sm">Read</label>
                        </div>
                        <div>
                            <input type="checkbox" id="write" name="permissions[]" value="write" class="mr-2">
                            <label for="write" class="text-sm">Write</label>
                        </div>
                        <div>
                            <input type="checkbox" id="delete" name="permissions[]" value="delete" class="mr-2">
                            <label for="delete" class="text-sm">Delete</label>
                        </div>
                    </div>
                </div>

                <!-- Shareable Toggle -->
                <div class="flex items-center">
                    <input type="checkbox" id="isShareable" name="isShareable" class="mr-2">
                    <label for="isShareable" class="text-sm font-medium text-gray-700">Enable Shareable Link</label>
                </div>

                <!-- User ID (Hidden) -->
                <input type="hidden" id="userId" name="userId" value="{{userId}}">

                <!-- Submit Button -->
                <div>
                    <button type="submit" class="w-full bg-black text-white font-semibold py-3 rounded-lg hover:bg-gray-800 transition">
                        Upload File
                    </button>
                </div>
            </form>
        </div>
    </div>

</body>
</html>
